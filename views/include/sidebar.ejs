<!-- Sidebar -->
<div id="sidebar" class="fixed lg:static inset-y-0 left-0 z-50 w-64 bg-gray-900 text-white sidebar-transition transform -translate-x-full lg:translate-x-0">
  <!-- Logo Area -->
  <div class="h-16 flex items-center justify-start border-b border-gray-800 pl-6">
    <i class="fas fa-wallet w-6 h-6 mr-2 text-blue-400"></i>
    <span class="text-xl font-bold">FinanceTracker</span>
  </div>

  <!-- Mobile Close Button -->
  <div class="lg:hidden absolute top-4 right-4">
    <button id="closeSidebar" class="text-gray-400 hover:text-white">
      <i class="fas fa-times text-xl"></i>
    </button>
  </div>

  <!-- Navigation Menu -->
  <nav class="mt-6 px-3">
    <!-- Dashboard -->
    <a href="/dashboard" class="mb-1 px-3 py-3 flex items-center space-x-3 cursor-pointer transition-all duration-200 <%= title === 'Dashboard' ? 'bg-gray-800' : '' %> hover:bg-gray-800 text-white rounded-lg">
      <i class="fas fa-home w-5 h-5"></i>
      <span>Dashboard</span>
    </a>

    <!-- Accounts -->
    <a href="/accounts" class="mb-1 px-3 py-3 flex items-center space-x-3 cursor-pointer transition-all duration-200 text-gray-300 <%= title === 'Accounts' ? 'bg-gray-800' : '' %> hover:bg-gray-800 hover:text-white rounded-lg">
      <i class="fas fa-user w-5 h-5"></i>
      <span>Accounts</span>
    </a>

    <a href="/transactions" class="mb-1 px-3 py-3 flex items-center space-x-3 cursor-pointer transition-all duration-200 text-gray-300 <%= title === 'Transactions' ? 'bg-gray-800' : '' %> hover:bg-gray-800 hover:text-white rounded-lg">
      <i class="fas fa-money-bill-transfer w-5 h-5"></i>
      <span>Transactions</span>
    </a>

    <!-- Transactions with Dropdown -->
    <!-- <div x-data="{ open: <%= title === 'Transactions' || title === 'Add Transaction' ? 'true' : 'false' %> }">
            <button @click="open = !open"
                class="w-full mb-1 px-3 py-3 flex items-center justify-between space-x-3 text-gray-300 hover:bg-gray-800 hover:text-white cursor-pointer transition-all duration-200 rounded-lg">
                <div class="flex items-center space-x-3">
                    <i class="fas fa-money-bill-transfer w-5 h-5"></i>
                    <span>Transactions</span>
                </div>
                <i class="fas" :class="open ? 'fa-chevron-up' : 'fa-chevron-down'"></i>
            </button>

            <div x-show="open" x-cloak class="ml-8">
                <a href="/transactions/add"
                    class="block px-3 py-2 text-sm text-gray-300 rounded hover:bg-gray-800 hover:text-white <%= title === 'Add Transaction' ? 'bg-gray-800 text-white' : '' %>">
                    Add Transaction
                </a>
                <a href="/transactions"
                    class="block px-3 py-2 text-sm text-gray-300 rounded hover:bg-gray-800 hover:text-white <%= title === 'Transactions' ? 'bg-gray-800 text-white' : '' %>">
                    View All
                </a>
            </div>
        </div> -->


    <!-- Budget -->
    <a href="#" class="mb-1 px-3 py-3 flex items-center space-x-3 text-gray-300 <%= title === 'Budget' ? 'bg-gray-800' : '' %> hover:bg-gray-800 hover:text-white cursor-pointer transition-all duration-200 rounded-lg">
      <i class="fas fa-money-bill w-5 h-5"></i>
      <span>Budget</span>
    </a>

    <!-- Investments -->
    <a href="#" class="mb-1 px-3 py-3 flex items-center space-x-3 text-gray-300 <%= title === 'Investments' ? 'bg-gray-800' : '' %> hover:bg-gray-800 hover:text-white cursor-pointer transition-all duration-200 rounded-lg">
      <i class="fas fa-piggy-bank w-5 h-5"></i>
      <span>Investments</span>
    </a>

    <!-- Goals -->
    <a href="#" class="mb-1 px-3 py-3 flex items-center space-x-3 text-gray-300 <%= title === 'Goals' ? 'bg-gray-800' : '' %> hover:bg-gray-800 hover:text-white cursor-pointer transition-all duration-200 rounded-lg">
      <i class="fas fa-archive w-5 h-5"></i>
      <span>Goals</span>
    </a>

    <!-- Reports -->
    <a href="#" class="mb-1 px-3 py-3 flex items-center space-x-3 text-gray-300 <%= title === 'Reports' ? 'bg-gray-800' : '' %> hover:bg-gray-800 hover:text-white cursor-pointer transition-all duration-200 rounded-lg">
      <i class="fas fa-list w-5 h-5"></i>
      <span>Reports</span>
    </a>

    <!-- Bills & Subscriptions -->
    <a href="#" class="mb-1 px-3 py-3 flex items-center space-x-3 text-gray-300 <%= title === 'Bills & Subscriptions' ? 'bg-gray-800' : '' %> hover:bg-gray-800 hover:text-white cursor-pointer transition-all duration-200 rounded-lg">
      <i class="fas fa-file-invoice-dollar w-5 h-5"></i>
      <span>Bills & Subs</span>
    </a>

    <div x-data="{ open: <%= title === 'Master' || title === 'Add Category' ? 'true' : 'false' %> }">
      <button @click="open = !open" class="w-full mb-1 px-3 py-3 flex items-center justify-between space-x-3 text-gray-300 hover:bg-gray-800 hover:text-white cursor-pointer transition-all duration-200 rounded-lg">
        <div class="flex items-center space-x-3">
          <i class="fas fa-money-bill-transfer w-5 h-5"></i>
          <span>Master</span>
        </div>
        <i class="fas" :class="open ? 'fa-chevron-up' : 'fa-chevron-down'"></i>
      </button>

      <div x-show="open" x-cloak class="ml-8">
        <a href="/masters/category/add" class="block px-3 py-2 text-sm text-gray-300 rounded hover:bg-gray-800 hover:text-white <%= title === 'Add Master' ? 'bg-gray-800 text-white' : '' %>">
          <i class="fas fa-plus ml-1"></i>Add Category
        </a>
        <!-- <a href="/masters/category/add" class="block px-3 py-2 text-sm text-gray-300 rounded hover:bg-gray-800 hover:text-white <%= title === 'Masters' ? 'bg-gray-800 text-white' : '' %>">
          View All
        </a> -->
      </div>
    </div>

    <div class="border-t border-gray-800 mt-6 pt-4">
      <!-- Settings -->
      <a href="#" class="mb-1 px-3 py-3 flex items-center space-x-3 text-gray-300 <%= title === 'Settings' ? 'bg-gray-800' : '' %> hover:bg-gray-800 hover:text-white cursor-pointer transition-all duration-200 rounded-lg">
        <i class="fas fa-gear w-5 h-5"></i>
        <span>Settings</span>
      </a>

      <!-- Help -->
      <a href="#" class="mb-1 px-3 py-3 flex items-center space-x-3 text-gray-300 <%= title === 'Help' ? 'bg-gray-800' : '' %> hover:bg-gray-800 hover:text-white cursor-pointer transition-all duration-200 rounded-lg">
        <i class="fas fa-question-circle w-5 h-5"></i>
        <span>Help</span>
      </a>
    </div>
  </nav>

  <!-- User Profile Area -->
  <div class="absolute bottom-0 w-64 border-t border-gray-800">
    <div class="px-6 py-4 flex items-center space-x-3">
      <div class="w-8 h-8 rounded-full bg-gradient-to-br from-blue-400 to-purple-500"></div>
      <div class="flex-1 min-w-0">
        <p class="text-sm font-medium truncate">John Doe</p>
        <p class="text-xs text-gray-400 truncate">Premium Member</p>
      </div>
      <button id="logout-btn" class="text-gray-400 hover:text-white">
        <i class="fas fa-sign-out-alt"></i>
      </button>
    </div>
  </div>
</div>